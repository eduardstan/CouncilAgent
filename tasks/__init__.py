"""Task loaders for CouncilAgent benchmark evaluation.

Each loader returns a list of BenchmarkTask instances from a standard dataset.
Loaders are registered in tasks.registry and consumed by experiments/run.py.
"""
