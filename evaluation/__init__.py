"""Evaluation layer — metrics, baselines, statistical estimators, Shapley values.

This package is benchmark-mode only. None of its modules are imported by council/.
council/ modules may be imported here (evaluation is downstream of the pipeline,
not a peer layer — see architecture.md "Approved exception: evaluation → council").
"""
