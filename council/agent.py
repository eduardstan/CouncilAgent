"""CouncilAgent — drop-in LLM interface. Implemented in Phase 3.

Kept as a stub in Phase 1 so that `mypy --strict council/agent.py` has something
to type-check and so that the strict-module whitelist in pyproject.toml resolves.
"""

__all__: list[str] = []
